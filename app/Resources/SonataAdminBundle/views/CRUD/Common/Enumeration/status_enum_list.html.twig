{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    <div>
        {% if value is not empty %}

            <div class="label label-{{ value.labelType }}">{{ value.readable|trans }}</div>

        {% endif %}
    </div>
{% endblock %}
