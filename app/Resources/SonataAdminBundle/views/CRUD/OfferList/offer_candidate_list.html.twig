{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    <div>
        <a href="{{ path('offer_candidate_list', {'id': object.id }) }}" class="btn btn-default">Jelentkezők</a>
    </div>
{% endblock %}